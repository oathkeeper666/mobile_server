#ifndef _CORE_H
#define _CORE_H

#include "config.h"
#include "palloc.h"
#include "array.h"
#include "list.h"
#include "queue.h"
#include "buffer.h"
#include "rbtree.h"

#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define MIN(a, b) ((a) < (b) ? (a) : (b))
#define ABS(a) ((a) >= 0 ? (a) : -(a))

#endif /* _CORE_H */
